# Development-only dependencies (audit F-C2-05): NOT installed in the
# production Docker image. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1

# Linter — config in pyproject.toml [tool.ruff]. Run: python -m ruff check .
ruff==0.15.22
